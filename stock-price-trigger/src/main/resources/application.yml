spring:
 application:
  name: stock-price-trigger

 data:
  mongodb:
   authentication-database: admin
   username: test
   password: test
   database: stock-trading-user
   

server:
 port: 9086

eureka:
 client:
  register-with-eureka: true
  fetch-registry: true
  serviceUrl:
   defaultZone: http://localhost:9080/eureka/

instance:
 hostname: localhost

